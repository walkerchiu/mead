# Frontend 環境變數 - Dev 環境
# 複製此檔案為 .env 並填入實際值

# ==============================================
# GraphQL API Endpoint
# ==============================================
# HTTP Endpoint for queries and mutations
NEXT_PUBLIC_GRAPHQL_ENDPOINT=https://dev-api.yourapp.com/graphql

# WebSocket Endpoint for subscriptions (如不使用 GraphQL Subscriptions 可保持空白)
NEXT_PUBLIC_GRAPHQL_WS_ENDPOINT=wss://dev-api.yourapp.com/graphql

# ==============================================
# Apollo Client Configuration
# ==============================================
# Request timeout in milliseconds (default: 30000)
# Dev 環境使用較短的超時時間以便快速發現問題
NEXT_PUBLIC_APOLLO_TIMEOUT=30000

# Maximum retry attempts (default: 3)
NEXT_PUBLIC_APOLLO_MAX_RETRIES=3

# Initial retry delay in milliseconds (default: 300)
NEXT_PUBLIC_APOLLO_RETRY_INITIAL_DELAY=300

# Maximum retry delay in milliseconds (default: 10000)
NEXT_PUBLIC_APOLLO_RETRY_MAX_DELAY=10000

# ==============================================
# 應用程式配置
# ==============================================
NEXT_PUBLIC_APP_NAME=Starter (Dev)
NEXT_PUBLIC_APP_URL=https://dev.yourapp.com

# ==============================================
# 功能開關
# ==============================================
NEXT_PUBLIC_ENABLE_2FA=true
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# ==============================================
# 錯誤追蹤 (Sentry)
# ==============================================
# Sentry DSN - 留空則不啟用 Sentry
# 從 https://sentry.io 取得您的 DSN
NEXT_PUBLIC_SENTRY_DSN=

# 應用程式版本資訊 (用於錯誤追蹤)
NEXT_PUBLIC_APP_VERSION=0.1.0
NEXT_PUBLIC_BUILD_ID=
NEXT_PUBLIC_COMMIT_SHA=
